---
name: Insert path — page sink
status: closed
created: 2026-08-17T07:06:06Z
updated: 2026-08-17T07:06:06Z
github: (local)
depends_on: [5]
parallel: true
conflicts_with: [7]
---

# Task: Insert path — page sink

## Description
`beginInsert`/`finishInsert`, `KubernetesPageSinkProvider`,
`KubernetesPageSink`: Trino row values → JSON object (shared
`JsonValueEncoder`, inverse of decoder; null pruning; apiVersion/kind filled
if absent) → POST create. Namespaced objects default namespace from config.

## Acceptance Criteria
- [ ] `INSERT INTO core.configmaps (metadata, data) VALUES (...)` creates a real object (verified via REST)
- [ ] Insert of nested row (pod with containers) round-trips

## Effort Estimate
- Size: M
