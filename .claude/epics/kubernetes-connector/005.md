---
name: Read path — metadata, splits, page source, pushdown
status: open
created: 2026-08-17T07:06:06Z
updated: 2026-08-17T07:06:06Z
github: (local)
depends_on: [4]
parallel: false
conflicts_with: [4]
---

# Task: Read path — metadata, splits, page source, pushdown

## Description
`KubernetesMetadata` (schemas=groups incl. `core` alias, tables=plurals,
columns from type mapper + hidden `$row_id`), `KubernetesTableHandle`,
`applyFilter` pushing `metadata.namespace`/`metadata.name` equality,
`KubernetesSplitManager` (single split), `KubernetesPageSource` decoding
JSON objects → Blocks (shared `JsonValueDecoder`), pagination via continue
tokens, limit pushdown.

## Acceptance Criteria
- [ ] SHOW SCHEMAS/TABLES, DESCRIBE, SELECT with nested dereference and UNNEST(containers) filter on image — green against envtest
- [ ] Namespace/name predicates result in scoped API calls (assert via request paths)

## Effort Estimate
- Size: L
