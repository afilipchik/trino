---
name: Update and Delete via merge SPI
status: closed
created: 2026-08-17T07:06:06Z
updated: 2026-08-17T07:06:06Z
github: (local)
depends_on: [5]
parallel: true
conflicts_with: [6]
---

# Task: Update and Delete via merge SPI

## Description
`getMergeRowIdColumnHandle` (synthetic varchar row-id: JSON
namespace/name/resourceVersion emitted by page source),
`getRowChangeParadigm`=FULL_ROW, `beginMerge`/`finishMerge`,
`KubernetesMergeSink` handling operation channel: DELETE→DELETE call,
UPDATE→PUT replace (new row JSON + original name/namespace/resourceVersion),
INSERT→POST.

## Acceptance Criteria
- [ ] `UPDATE ... SET data = ... WHERE metadata.name = ...` patches object (verified via REST)
- [ ] `DELETE FROM ... WHERE` with arbitrary predicate (e.g. on labels map) deletes only matching objects
- [ ] Immutable fields (metadata.name change) produce a clear error

## Effort Estimate
- Size: L
