---
name: TestingKubernetesCluster harness (envtest)
status: closed
created: 2026-08-17T07:06:06Z
updated: 2026-08-17T07:06:06Z
github: (local)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: TestingKubernetesCluster harness (envtest)

## Description
Test-scope class that launches `etcd` + `kube-apiserver` (envtest binaries,
located via `TESTING_KUBERNETES_ASSETS` env var or system property, default
`/media/afilipchik/nvme6tb/src/local/.tools/controller-tools/envtest`) on
random free ports with static-token auth (`--token-auth-file`),
`--authorization-mode=AlwaysAllow`, generated SA signing key (openssl).
Exposes URI + token + Closeable lifecycle; helper to apply raw JSON/YAML
manifests (direct REST, no kubectl dependency in tests).

## Acceptance Criteria
- [ ] JUnit test starts the cluster, creates + lists a configmap via REST, shuts down cleanly
- [ ] Startup < 30s, robust readiness wait on `/readyz`

## Effort Estimate
- Size: M
