---
name: Documentation and repo validation
status: open
created: 2026-08-20T03:41:33Z
updated: 2026-08-20T03:41:33Z
github: (local only)
depends_on: [007]
parallel: true
conflicts_with: []
---

# Task: Documentation and repo validation

## Description

Connector documentation page and repo-level hygiene so the module is a first-class citizen.

## Acceptance Criteria

- [ ] `docs/src/main/sphinx/connector/federation.md` following existing connector doc structure:
      overview, requirements, configuration table (all `federation.*` properties), `_region`
      column, pushdown section (what is pushed, aggregation fan-out semantics, fallback rules),
      limitations (read-only, homogeneous shards, same-version regions)
- [ ] Doc registered in the connector doc index/toctree
- [ ] `./mvnw validate` passes repo-wide
- [ ] `./mvnw -pl plugin/trino-federation install` with checks on (no
      `-Dair.check.skip-all`) passes: checkstyle, modernizer, license headers, pom sortedness
- [ ] mcp__idea__get_file_problems (if IDE available) or equivalent inspection pass on new files

## Technical Details

- Mirror the kubernetes connector doc from the previous epic for tone/structure.
- Verify provisio + root pom entries from task 001 survive `validate` (sortpom ordering).

## Dependencies

007 (feature-complete surface to document). Parallel with 008/010.

## Effort Estimate

- Size: S
- Hours: 3

## Definition of Done

- [ ] Docs build clean
- [ ] Validation green
